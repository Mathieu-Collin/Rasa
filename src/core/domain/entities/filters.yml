version: "3.1"

entities:
    - kpi
    - sex
    - stroke_type
    - boolean_type
    - group_by
    - chart_type
    - nihss:
          roles: [lower, upper]
    - age:
          roles: [lower, upper]
    - date:
          roles: [lower, upper]
