action_endpoint:
  url: "http://action_devcontainer-action-1:5055/webhook"
lock_store:
  type: redis
  url: ${LOCK_STORE_URL}
  db: ${LOCK_STORE_DB}
tracker_store:
  type: redis
  url: ${TRACKER_STORE_URL}
  db: ${TRACKER_STORE_DB}
cors:
  enabled: true
  allowed_origins:
    - "*"
